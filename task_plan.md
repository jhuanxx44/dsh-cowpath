# Cowpath MVP 执行计划

## 目标
实现一个零依赖、可离线复跑的弯路候选报告器：从 DSH `session.jsonl.zstd` 清单中提取会话上下文、按操作对象配对失败→修正→成功链，确定性过滤噪声，并输出 JSON/Markdown 候选报告。

## 阶段
- [complete] 1. 规划 MVP 接口与数据边界
- [complete] 2. 实现会话解析、对象提取、过滤与配对
- [complete] 3. 用合成样本和真实会话回放验证
- [complete] 4. 更新文档与记录限制

## 验收标准
- 不同 session 的 turn/step 绝不互相配对
- 支持 `.zstd` 清单和单条路径；只使用 Python 标准库与系统 zstd
- 输出可机器消费的 JSON，并可选 Markdown
- 候选包含失败签名、修正动作、对象、session 证据和过滤原因
- 对真实当前数据可运行并报告统计

## 错误

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `trail_scan.py --help` 被当成清单路径并触发 `FileNotFoundError` | 1 | 为扫描器加入 `argparse` CLI；现在支持 `--help` |
| 临时合成测试的清理命令被执行器拒绝（`rm -f style commands are not permitted`） | 1 | 不影响仓库代码；改用不删除临时目录的验证命令完成测试 |

## 后续推进路线（2026-09-14）

> 约束：在完成 3–5 轮真实任务闭环并确认“可识别弯路”前，不写 DSH 实时插件骨架。

- [in_progress] 5. 冻结并修正离线测量口径
  - 修复 `trail_scan.py` 的跨 session 状态污染、`timed out` 环境分类和脚本输入路径不一致。
  - 将每次扫描的清单哈希、脚本版本/提交、会话数、错误数和配对规则写入报告元数据。
  - 验收：同一清单重复运行结果字节一致；不同 session 不可能互相配对。
  - 已完成：跨 session 状态隔离、环境关键词修正、参数化 zstd 解压、输入路径 CLI 化；待补：报告元数据中的清单哈希和脚本版本。
- [next] 6. 按操作对象复算候选池
  - 统一提取文件路径/目标对象，识别跨工具的“失败 → 修正 → 成功”链。
  - 输出候选分组、独立 session 数、原始证据和过滤原因；人工抽检至少 20 条。
  - 验收：明确区分会话上下文噪声、自带修复指令、协议弯路和领域弯路。
- [next] 7. 手工跑 3–5 轮闭环（前提验证门）
  - 从候选池挑一个非自解释、可重复的真实任务类型，人工写 B 臂 `SKILL.md`，执行 lint 并记录 A/B 指标。
  - 每轮保留任务输入、skill 版本、工具错误、返工轮数、cost、latency 和人工抽检结论。
  - 验收：至少 3 轮有可回指证据；若没有稳定弯路，停止自动蒸馏方向并记录否证结果。
- [next] 8. 小规模 A/B/C 三臂实验
  - A 裸跑，B 人工 skill，C 仅使用经人工确认的 Cowpath 候选；同一任务多次运行，避免 n=1。
  - 先比较成功率、错误数和返工，再看 cost/latency；C 必须与 B 的 skill 内容和验证规则可追溯。
  - 验收：冻结任务集、运行配置、模型/提示词、指标分母和原始结果齐全。
- [next] 9. 运行时插件设计（仅当前提验证通过后）
  - 先写观察层、事件边界、工作区隔离、权限/隐私、持久化和失败恢复的接口规格。
  - 再实现最小后台观察器；蒸馏、自动写入和回滚放在验证门之后。
  - 验收：实时事件回放与离线 MVP 对同一轨迹给出一致候选，且人工确认仍是写入门。
